{% extends 'base.html.twig' %}

{% block body %}
    <h1>tout les avis</h1>

    <table>
        <thead>
            <tr>
                <th>Avis sur ...</th>
                <th>Écrit par ...</th>
                <th>date de publication</th>
                <th>note</th>
                <th>Avis</th>
            </tr>
        </thead>
        <tbody>
        {% for review in reviews %}
            <tr>
              {#  <td><a href="{{ path('review_show', { 'id': review.id }) }}">{{ review.id }}</a></td>#}
                <td>{{ review.userRated }}</td>
                <td>{{ review.reviewAuthor }}</td>
                <td>{{ review.publicationDate|date('d/m/Y H\\hi\\ms') }}</td>
                <td>{{ review.note }}</td>
                <td>{{  review.Text }}</td>
                <td>
                    <ul>
{#
                        <li>
                            <a href="{{ path('review_show', { 'id': review.id }) }}">show</a>
                        </li>
#}
                       {# <li>
                            <a href="{{ path('review_edit', { 'id': review.id }) }}">edit</a>
                        </li>#}
                    </ul>
                </td>
            </tr>


        {% endfor %}

         </tbody>
    </table>

    <div>
        <a href="{{  path('review_new') }}">Nouvelliser une revuation</a>
    </div>

{% endblock %}
